타입을 나중에 지정할 수 있게 만든 문법. 타입 안정성과 재사용성을 높인다.

## 코드 예시

```csharp
List<string> names = new() { "Alice", "Bob" };
Console.WriteLine(names[0]);
```

## 이해를 돕는 설명

타입별로 같은 코드를 반복하지 않으면서도 컴파일 단계의 타입 검사를 유지한다. List<T>, Dictionary<TKey,TValue>와 공용 팩토리에서 대표적으로 사용된다.
