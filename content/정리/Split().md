문자열을 특정 기호나 문자를 기준으로 나누는 메서드.

## 코드 예시

```csharp
string csv = "red,green,blue";
string[] colors = csv.Split(',');
```

## 이해를 돕는 설명

CSV처럼 구분자가 단순한 데이터에는 편하지만 따옴표와 이스케이프 규칙이 있으면 전용 파서가 필요하다. 빈 항목 제거 여부도 옵션으로 결정할 수 있다.
