한 번 정해지면 값을 바꿀 수 없는 데이터. C#에서는 const나 readonly로 표현한다.

## 코드 예시

```csharp
const int MaxLevel = 99;
Console.WriteLine(MaxLevel);
```

## 이해를 돕는 설명

마법 숫자를 의미 있는 이름의 상수로 바꾸면 값의 목적이 드러난다. const는 컴파일 타임 값, readonly는 실행 중 생성자에서 결정할 값에 적합하다.
