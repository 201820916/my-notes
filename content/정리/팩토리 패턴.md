객체 생성을 별도 클래스나 메서드에 맡기는 생성 패턴.

## 코드 예시

```csharp
IEnemy CreateEnemy(string type) => type switch
{
    "Slime" => new Slime(),
    _ => new Goblin()
};
```

## 이해를 돕는 설명

호출자는 어떤 구체 클래스를 new 하는지 몰라도 된다. 생성 규칙이 복잡하거나 플랫폼과 데이터에 따라 구현이 달라질 때 효과가 크다.
