마지막에 들어온 데이터가 먼저 나가는 LIFO 자료구조.

## 코드 예시

```csharp
Stack<string> undo = new();
undo.Push("Move");
string last = undo.Pop();
```

## 이해를 돕는 설명

실행 취소, 화면 전환 기록, 재귀 탐색처럼 최근 작업을 먼저 처리할 때 적합하다. Push로 넣고 Pop으로 가장 최근 항목을 꺼낸다.
