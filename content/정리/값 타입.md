값 자체를 변수에 저장하는 타입. int, float, bool, struct 등이 대표적이다.

## 코드 예시

```csharp
int a = 10;
int b = a;
b = 20;
Console.WriteLine(a); // 10
```

## 이해를 돕는 설명

다른 변수에 대입하면 값이 복사되므로 한쪽을 바꿔도 원본은 변하지 않는다. 큰 struct를 자주 복사하면 비용이 커질 수 있어 작은 데이터 묶음에 주로 사용한다.
