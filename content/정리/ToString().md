객체를 문자열 형태로 표현할 때 사용하는 메서드.

## 코드 예시

```csharp
int score = 42;
string message = score.ToString();
Console.WriteLine(message);
```

## 이해를 돕는 설명

로그와 UI에 객체 정보를 표시할 때 유용하다. 사용자 정의 타입에서 재정의하면 클래스 이름 대신 체력이나 좌표처럼 의미 있는 내용을 보여줄 수 있다.
