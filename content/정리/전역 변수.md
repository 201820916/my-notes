프로그램의 넓은 범위에서 접근 가능한 변수. 편하지만 남용하면 코드 관리가 어려워진다.

## 코드 예시

```csharp
class Player
{
    private int score; // 클래스 전체에서 사용

    void AddScore(int value) => score += value;
}
```

## 이해를 돕는 설명

어디서든 값을 바꿀 수 있으면 변경 원인을 찾기 어려워진다. 읽기 전용 접근이나 관리자 객체, 의존성 주입 등으로 접근 범위를 제한하는 편이 안전하다.
