여러 값을 하나로 묶어 간단히 반환하거나 전달할 때 쓰는 자료형.

## 코드 예시

```csharp
(string name, int score) player = ("Alice", 100);
Console.WriteLine(player.score);
```

## 이해를 돕는 설명

한 번만 쓰는 작은 결과 묶음에는 편하지만 의미 있는 도메인 데이터라면 이름 있는 class나 struct가 더 이해하기 쉽다. 튜플 요소에도 의미 있는 이름을 붙이는 것이 좋다.
