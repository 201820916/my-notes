객체를 만들지 않고 클래스 단위로 공유되는 멤버. 모든 인스턴스가 같은 값을 공유한다.

## 코드 예시

```csharp
class Score
{
    public static int Total;
}

Score.Total += 10;
```

## 이해를 돕는 설명

공유 상태를 쉽게 만들 수 있지만 테스트와 초기화 순서가 복잡해질 수 있다. 상태가 없는 유틸리티 함수나 정말 하나로 공유해야 하는 값에 제한해 사용하는 편이 좋다.
