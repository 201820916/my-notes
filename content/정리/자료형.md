데이터가 어떤 종류의 값인지 나타내는 분류. int, float, bool, string 등이 있다.

## 코드 예시

```csharp
int count = 3;
float speed = 5.5f;
bool isReady = true;
string playerName = "Codex";
```

## 이해를 돕는 설명

자료형은 저장 가능한 값뿐 아니라 사용할 수 있는 연산도 결정한다. 같은 숫자라도 int는 정수 계산, float는 소수 계산에 적합하며 정밀도와 범위가 다르다.
