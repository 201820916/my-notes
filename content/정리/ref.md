변수를 메서드에 참조로 전달해, 메서드 내부 변경이 호출한 쪽에도 반영되게 하는 키워드.

## 코드 예시

```csharp
void AddOne(ref int value) => value++;
int score = 10;
AddOne(ref score); // 11
```

## 이해를 돕는 설명

복사 비용이 큰 값을 전달하거나 호출자 변수를 직접 바꿀 때 쓰지만 데이터 흐름이 숨겨질 수 있다. 단순한 결과 반환에는 반환값을 우선 고려하는 편이 읽기 좋다.
