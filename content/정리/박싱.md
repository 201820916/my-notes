값 타입 데이터를 object 같은 참조 타입으로 감싸 힙에 저장하는 과정.

## 코드 예시

```csharp
int number = 10;
object boxed = number; // 값 타입을 object로 변환
```

## 이해를 돕는 설명

값 타입을 object나 인터페이스로 취급할 때 새 객체 할당이 발생할 수 있다. 반복 루프에서 박싱이 쌓이면 GC 부담이 되므로 제네릭 컬렉션을 사용하는 편이 좋다.
