한 객체의 변화가 여러 구독자에게 자동으로 전달되는 행동 패턴.

## 코드 예시

```csharp
health.OnChanged += value => hpText.text = value.ToString();
```

## 이해를 돕는 설명

발행자는 구독자의 구체 타입을 몰라도 변화를 알릴 수 있다. 알림 순서, 중복 구독, 해제 시점을 관리하지 않으면 디버깅이 어려워질 수 있다.
