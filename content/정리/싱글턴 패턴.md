애플리케이션 전체에서 특정 클래스의 인스턴스를 하나만 만들도록 보장하는 패턴.

## 코드 예시

```csharp
class GameManager
{
    public static GameManager Instance { get; } = new();
}
```

## 이해를 돕는 설명

접근은 편하지만 전역 상태와 숨은 의존성을 만든다. 수명과 초기화 순서가 분명한 정말 하나뿐인 서비스에 제한해 사용하는 것이 좋다.
