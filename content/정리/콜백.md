특정 작업이 끝났거나 특정 시점이 되었을 때 다시 호출되는 함수.

## 코드 예시

```csharp
void Load(Action onComplete)
{
    // 로드 작업 후
    onComplete();
}

Load(() => Console.WriteLine("완료"));
```

## 이해를 돕는 설명

완료 시점을 호출자에게 알려 비동기 작업이나 UI 반응을 연결한다. 콜백이 여러 단계로 중첩되면 흐름이 복잡해져 async/await나 이벤트가 더 적합할 수 있다.
