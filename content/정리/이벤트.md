델리게이트 기반으로 특정 일이 발생했음을 외부에 알리는 구조.

## 코드 예시

```csharp
public event Action<int>? OnHpChanged;

void TakeDamage(int amount)
{
    OnHpChanged?.Invoke(amount);
}
```

## 이해를 돕는 설명

외부 코드는 구독과 해지만 할 수 있고 이벤트 발생은 선언한 클래스만 수행한다. 파괴된 객체의 구독을 해제하지 않으면 예상치 못한 호출이나 참조 유지 문제가 생길 수 있다.
