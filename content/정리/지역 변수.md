함수나 블록 내부에서 선언되어 그 범위 안에서만 사용할 수 있는 변수.

## 코드 예시

```csharp
void PrintScore()
{
    int score = 10; // 이 함수 안에서만 사용
    Console.WriteLine(score);
}
```

## 이해를 돕는 설명

사용 범위가 좁아 다른 코드에 영향을 주기 어렵고 수명이 짧다. 가능한 한 필요한 블록 가까이에서 선언하면 데이터 흐름을 추적하기 쉽다.
