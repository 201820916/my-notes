저장된 문자열이나 파일 데이터를 다시 코드 객체로 복원하는 과정.

## 코드 예시

```csharp
PlayerData data = JsonUtility.FromJson<PlayerData>(json);
```

## 이해를 돕는 설명

파일이 오래되었거나 필드가 누락될 수 있으므로 기본값과 버전 호환성을 고려해야 한다. 외부에서 받은 데이터는 신뢰하지 말고 유효성 검사를 거친다.
