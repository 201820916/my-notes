변수는 private로 유지하면서 유니티 인스펙터에서 값을 수정할 수 있게 하는 속성.

## 코드 예시

```csharp
[SerializeField] private float moveSpeed = 5f;
```

## 이해를 돕는 설명

캡슐화를 유지하면서 에디터에서 값을 연결할 수 있다. 인스펙터에 저장된 값이 코드의 필드 초기값보다 우선할 수 있다는 점을 주의한다.
