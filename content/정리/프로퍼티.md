필드 값을 안전하게 읽고 쓰기 위한 C# 문법. get/set으로 접근을 제어한다.

## 코드 예시

```csharp
class Player
{
    public int Hp { get; private set; } = 100;
}
```

## 이해를 돕는 설명

외부에는 값처럼 보이지만 내부에서 검증이나 알림 로직을 넣을 수 있다. 단순 필드 공개보다 변경 권한을 세밀하게 제한할 수 있다.
