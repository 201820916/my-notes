같은 이름의 메서드를 매개변수 차이로 여러 개 정의하는 것.

## 코드 예시

```csharp
void Heal(int amount) { }
void Heal(int amount, bool showEffect) { }
```

## 이해를 돕는 설명

컴파일러는 전달한 인수의 개수와 타입을 보고 어떤 메서드를 부를지 결정한다. 반환형만 다른 메서드는 오버로드할 수 없다.
