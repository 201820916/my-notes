값을 저장하는 이름 붙은 공간. 자료형에 맞는 값을 담아 사용한다.

## 코드 예시

```csharp
int score = 10;
score += 5;
Console.WriteLine(score); // 15
```

## 이해를 돕는 설명

이름에는 값의 의미와 단위를 드러내는 것이 좋다. speed보다 moveSpeed, time보다 elapsedSeconds처럼 지으면 코드를 다시 읽을 때 오해가 줄어든다.
