값 타입으로 동작하는 사용자 정의 자료형. 작은 데이터 묶음에 적합하다.

## 코드 예시

```csharp
struct Damage
{
    public int Value;
}

Damage hit = new() { Value = 10 };
```

## 이해를 돕는 설명

복사될 때 별도 값이 되므로 좌표나 색처럼 작고 독립적인 데이터에 잘 맞는다. 큰 구조체나 자주 변경되는 공유 상태에는 클래스가 더 적합할 수 있다.
