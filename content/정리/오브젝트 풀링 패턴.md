자주 생성/삭제되는 객체를 미리 만들어 재사용하는 패턴.

## 코드 예시

```csharp
GameObject bullet = pool.Get();
bullet.SetActive(true);
```

## 이해를 돕는 설명

반납할 때 위치, 체력, 이벤트 구독 같은 이전 상태를 초기화해야 한다. 풀 크기는 최대 동시 사용량을 기준으로 잡고 부족할 때 정책도 정한다.
