박싱된 데이터를 원래 값 타입으로 다시 꺼내는 과정. 정확한 타입 캐스팅이 필요하다.

## 코드 예시

```csharp
object boxed = 10;
int number = (int)boxed;
```

## 이해를 돕는 설명

박싱할 때의 정확한 값 타입으로 꺼내야 하며 타입이 다르면 예외가 발생한다. object에 여러 타입을 섞어 담는 설계는 이런 오류를 만들기 쉽다.
