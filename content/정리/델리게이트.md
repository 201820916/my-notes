메서드를 변수처럼 담고 전달할 수 있게 하는 타입.

## 코드 예시

```csharp
Action<int> onScoreChanged = score => Console.WriteLine(score);
onScoreChanged(100);
```

## 이해를 돕는 설명

호출할 메서드의 형태를 타입으로 표현하며 여러 메서드를 연결할 수도 있다. Action과 Func는 자주 쓰는 델리게이트 형태를 미리 제공한다.
