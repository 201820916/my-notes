먼저 들어온 데이터가 먼저 나가는 FIFO 자료구조.

## 코드 예시

```csharp
Queue<string> waiting = new();
waiting.Enqueue("Player1");
string next = waiting.Dequeue();
```

## 이해를 돕는 설명

매칭 대기열, 명령 처리, BFS처럼 들어온 순서를 유지해야 할 때 적합하다. Enqueue로 넣고 Dequeue로 가장 오래된 항목을 꺼낸다.
