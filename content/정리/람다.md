이름 없는 함수를 짧게 표현하는 문법. 콜백이나 LINQ에서 자주 사용한다.

## 코드 예시

```csharp
List<int> values = new() { 1, 2, 3 };
var doubled = values.Select(x => x * 2);
```

## 이해를 돕는 설명

짧은 동작을 그 자리에서 전달할 수 있지만 내용이 길어지면 이름 있는 메서드가 읽기 쉽다. 캡처한 외부 변수 때문에 할당이나 수명 연장이 생길 수 있음도 기억해야 한다.
