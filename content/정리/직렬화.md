객체나 데이터를 파일, 문자열, 네트워크 전송이 가능한 형태로 바꾸는 과정.

## 코드 예시

```csharp
string json = JsonUtility.ToJson(new PlayerData { level = 3 });
```

## 이해를 돕는 설명

저장 대상의 모든 런타임 상태를 무조건 기록하기보다 복원에 필요한 데이터만 골라야 한다. Unity가 직렬화할 수 있는 타입과 일반 C# 직렬화 규칙은 차이가 있다.
