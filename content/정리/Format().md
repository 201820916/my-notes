지정한 자리표시자에 값을 넣어 문자열을 만드는 기능.

## 코드 예시

```csharp
string text = string.Format("HP: {0}/{1}", 50, 100);
```

## 이해를 돕는 설명

오래된 코드와 지역화 문자열에서 자주 보이며 자리표시자 번호가 값 순서와 맞아야 한다. 일반 코드에서는 문자열 보간이 더 읽기 쉬운 경우가 많다.
