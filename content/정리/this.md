현재 객체 자신을 가리키는 키워드.

## 코드 예시

```csharp
class Player
{
    int hp;
    public Player(int hp) { this.hp = hp; }
}
```

## 이해를 돕는 설명

매개변수와 필드 이름이 같을 때 둘을 구분하거나 현재 인스턴스를 다른 함수에 전달할 때 사용한다. 생략해도 뜻이 명확한 곳에서는 반드시 붙일 필요는 없다.
