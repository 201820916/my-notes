복잡한 연산과 게임 로직 처리를 담당하는 중앙 처리 장치.

## 코드 예시

```csharp
Debug.Log(SystemInfo.processorType);
```

## 이해를 돕는 설명

분기와 순차 로직에 강하지만 수많은 동일 계산을 한꺼번에 처리하는 데는 GPU보다 불리할 수 있다. 메인 스레드 병목인지 여러 코어 활용이 부족한지 프로파일링으로 구분한다.
