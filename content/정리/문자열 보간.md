문자열 안에 변수 값을 직접 끼워 넣는 문법. C#에서는 `$""` 형태를 사용한다.

## 코드 예시

```csharp
string name = "Player";
int score = 100;
string message = $"{name}: {score}";
```

## 이해를 돕는 설명

여러 값을 조합하는 로그와 UI 문구를 읽기 쉽게 만든다. 매 프레임 반복하면 새 문자열 할당이 생기므로 매우 빈번한 갱신에서는 비용을 확인해야 한다.
